# Camadas opcionais. O dashboard funciona sem nenhuma delas - o que muda e a
# moldura em volta:
#
#   sem nada          -> abre no browser
#   com pywebview     -> janela do sistema, sem barra de endereco,
#                        com "sempre no topo"
#   com pystray+PIL   -> icone de bandeja junto, com notificacao
#
# Instale as duas linhas para a experiencia completa:
pywebview>=5.0
pystray>=0.19.5
pillow>=10.0.0
